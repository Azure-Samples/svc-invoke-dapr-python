name: service-invoke-dapr-python-aca
metadata:
  template: service-invoke-dapr-python-aca@0.0.1-beta
services:
  worker:
    project: checkout
    language: python
    host: containerapp
    module: app/worker
  api:
    project: order-processor
    language: python
    host: containerapp
    module: app/api
 
